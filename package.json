{
	"name": "omp-a2a",
	"version": "0.1.0",
	"private": true,
	"description": "OMP multi-project custom mesh: independent Hub servers + pure client extension",
	"type": "module",
	"omp": {
		"extensions": ["./src/extension.ts"]
	},
	"bin": {
		"omp-a2a-hub": "./src/hub/cli.ts"
	},
	"scripts": {
		"hub": "bun run src/hub/cli.ts",
		"smoke": "bun test && bun run scripts/smoke.ts && bun run scripts/smoke-hub.ts"
	},
	"engines": {
		"bun": ">=1.1.0"
	},
	"dependencies": {
		"express": "^5.2.1"
	},
	"devDependencies": {
		"@types/express": "^5.0.6"
	}
}
